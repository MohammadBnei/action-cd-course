{
  "name": "action-cd-course",
  "version": "1.0.0",
  "description": "Create an action that logs Hello ${input.user} and outputs the time it has greeted",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mohammad Banaei",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.9.1",
    "@actions/github": "^5.0.3"
  }
}
